#!/bin/sh
#
# Execute buildReadme to copy name, description, and endpoints.json into README,
# formatting JSON into Markdown
# Execute buildGitInfo to populate gitInfo.json with project's git info
# Add the generated files to your commit
./buildReadme.sh
./buildGitInfo.sh
git add gitInfo.json
git add README.md
